name: Deploy to Vercel (Disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "Vercel deploy workflow disabled; using GitHub Pages only."
